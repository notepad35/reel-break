Opens Instagram Reels while Codex works and closes the tab when it stops.

Requires macOS and Google Chrome.

```sh
codex plugin marketplace add notepad35/reel-break
codex plugin add reel-break@reel-break
```

Run `/hooks` in Codex to review and trust the hooks, then start a new session. Allow Chrome automation if macOS asks.
